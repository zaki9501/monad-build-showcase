# Monad Community Builds Showcase - Environment Variables
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (Required for Vite/React App)
# =============================================================================

# Your Supabase project URL
# Get this from: Supabase Dashboard → Settings → API → Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (safe to expose in frontend)
# Get this from: Supabase Dashboard → Settings → API → anon public
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlvdXItcHJvamVjdC1pZCIsInJvbGUiOiJhbm9uIiwiaWF0IjoxNjM5NzQ5NjAwLCJleHAiOjE5NTUzMjU2MDB9.your-anon-key-here

# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (For Supabase Edge Functions Only)
# =============================================================================
# These should be set in Supabase Dashboard, NOT in your frontend .env file

# Supabase service role key (NEVER expose in frontend)
# Set this in: Supabase Dashboard → Settings → API → service_role
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Twitter API key for fetching user profiles
# Set this in: Supabase Dashboard → Settings → Edge Functions → Environment Variables
# TWITTER_API_KEY=your-twitter-api-key-here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Development mode (optional)
# NODE_ENV=development

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. Frontend variables (VITE_*) will be visible in the browser - this is OK for Supabase anon keys
# 2. Backend variables should only be set in Supabase Dashboard, not in your .env file
# 3. Never commit your actual .env.local file to version control
# 4. For production deployment, set these variables in your hosting platform (Vercel, Netlify, etc.)
#
# =============================================================================
